# AI Government Scheme Finder Environment Variables
PORT=5000
FLASK_DEBUG=True
SECRET_KEY=bujji-gov-scheme-secret-key-2026

# Optional: Google Gemini API Key for Natural Language Intelligence
# If left empty, deterministic rule-based regex extraction will be used automatically!
GEMINI_API_KEY=your_gemini_api_key_here

# Optional: MongoDB Connection URI (Falls back to local JSON database if MongoDB is inactive)
MONGO_URI=mongodb://localhost:27017/gov_scheme_db
